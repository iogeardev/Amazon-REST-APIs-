# Seller / Catalog

**Purpose:** Reference for Listings + Catalog Items snapshots.

## Primary outputs (recommended)
- Raw: `exports/raw/seller/...`
- Modeled: `exports/modeled/seller/...`

## Notes
- Replace stub JSON with real SP-API response snapshots (sanitized).
- Keep column naming consistent with `powerbi/schema/*.schema.json`.

